type: markdown
content: "# {{ title }}"
style: |
  ha-card {
    box-shadow: none;
    background: none;
  }
  ha-markdown {
    padding: 12px 0px 12px {{ padding_left | default('0px') }} !important;
  }
